[build]
  command = "npm run build"
  publish = "dist"  # Här publiceras den byggda Vite-applikationen

[functions]
  directory = "netlify/functions"  # Ange var dina serverless-funktioner finns

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/ssr"
  status = 200
